# Futur3Sn0w's Public Repo

This branch hosts the jailbreak package repo for Futur3Sn0w tweaks.

Add the GitHub Pages URL for this repository to Sileo, Zebra, Cydia, or another compatible package manager.

Package files live in `debs/`, and package indexes are generated as `Packages` and `Packages.gz`.

